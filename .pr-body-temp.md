## FLOW-BOOKING-CONFIRMATION-ROBUST-01 — R1: robustify confirmation page

### Plan item
- ID: FLOW-BOOKING-CONFIRMATION-ROBUST-01
- Risk: R1
- SSOT: docs/plan/IMPROVEMENTS.md

### Summary
Makes `/booking/confirmation` refresh-safe by adding sessionStorage fallback (coherent with Details.tsx pattern).

### Changes
- Import `loadBookingSession` from `@/lib/bookingSession`
- Read `location.state` with fallback to `sessionSnapshot?.bookingData`
- Preserves existing "No booking found" UI if data absent/TTL expired
- No guard changes (RequireBookingData already permits confirmation without data for fallback UI)

### Gates
- [x] npm run type-check
- [x] npm run build
- [x] eslint Confirmation.tsx

### R1 Manual Smoke (A/B/C)
- [ ] A) Happy path: payment → confirmation displays booking details
- [ ] B) Refresh confirmation page: data rehydrates from sessionStorage (TTL < 30min)
- [ ] C) TTL expired (>30min): shows "No booking found" + CTAs (no crash/loop)

### Diff
Single file: `src/pages/booking/Confirmation.tsx` (+2 lines: import + sessionSnapshot fallback)
